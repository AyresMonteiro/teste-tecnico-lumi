{
    "version": 2,
    "builds": [
        {
            "src": "backend/package.json",
            "use": "@now/node"
        }
    ],
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/backend/src/"
        }
    ]
}
